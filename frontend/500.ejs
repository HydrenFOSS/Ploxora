<!DOCTYPE html>
<html lang="en">
<%- include('components/head') %>
<body class="flex items-center justify-center min-h-screen bg-neutral-950 text-gray-300 font-[Figtree]">
  <div class="bg-neutral-800 border border-[#2a2a2a] p-10 rounded-2xl shadow-lg w-[480px] text-center">
    <h1 class="text-6xl font-bold text-red-500 mb-4">500</h1>
    <p class="text-xl text-white font-semibold">Something went wrong on our end.</p>
    <p class="text-gray-400 mb-6">Please try again later.</p>

    <% if (err) { %>
      <div class="text-left bg-neutral-900 border border-neutral-700 rounded-lg p-4 overflow-x-auto text-sm text-red-400">
        <strong>Error details (dev mode):</strong>
        <pre class="whitespace-pre-wrap break-words"><%= err %></pre>
      </div>
    <% } %>

    <a href="/" 
       class="mt-6 inline-flex items-center justify-center w-full px-6 py-3 bg-neutral-900 hover:bg-neutral-800 text-white font-semibold rounded-xl transition">
       Go Home
    </a>
  </div>
</body>
</html>
