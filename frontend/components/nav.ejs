<nav class="mt-3 bg-gradient-to-r from-[#111111] to-[#1a1a1a] p-4 flex justify-between items-center relative z-10 rounded-b-2xl shadow-lg border border-[#2a2a2a]">
  <!-- Left side: Logo/Name -->
  <h1 class="text-2xl font-bold tracking-wide flex items-center space-x-2">
    <span><%= name %> Dashboard</span>
  </h1>

  <!-- Right side: User info -->
  <div class="flex items-center space-x-4 bg-[#1a1a1a] px-3 py-1.5 rounded-2xl shadow-md border border-[#2a2a2a]">
    <img src="<%= user.profilePicture %>" 
         class="w-9 h-9 rounded-full border border-[#333] shadow-sm" 
         alt="Avatar">
    <div class="flex flex-col">
      <span class="text-gray-200 font-medium"><%= user.username %></span>
      <span class="text-xs text-green-500">Online</span>
    </div>
    <a href="/logout" 
       class="bg-[#ff4d4d] px-3 py-1 rounded-lg hover:bg-[#e04444] transition shadow">
       Logout
    </a>
  </div>
</nav>
